<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cms.system.mapper.SysDictTypeMapper">

    <!-- 映射表 -->
    <resultMap id="SysDictTypeResult" type="com.cms.system.api.domain.pojo.SysDictType">
        <id property="dictId" column="dict_id"/>
        <result property="dictName" column="dict_name"/>
        <result property="dictType" column="dict_type"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <!-- 查询列表数据 -->
    <select id="listEntities" resultType="com.cms.system.api.domain.pojo.SysDictType" resultMap="SysDictTypeResult">
        select dict_id,
        dict_name,
        dict_type,
        status,
        create_time
        from sys_dict_type
        <where>
            <if test="dictName != null and dictName != ''">
                and dict_name like concat('%',#{dictName},'%')
            </if>
            <if test="dictType != null and dictType != ''">
                and dict_type like concat('%',#{dictType},'%')
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="beginTime != null and endTime != null">
                and create_time between #{beginTime, jdbcType=TIMESTAMP} and #{endTime, jdbcType=TIMESTAMP}
            </if>
        </where>
    </select>
</mapper>
