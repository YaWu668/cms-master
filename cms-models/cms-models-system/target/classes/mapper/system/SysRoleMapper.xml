<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cms.system.mapper.SysRoleMapper">

    <!-- 字段映射 -->
    <resultMap type="com.cms.system.api.domain.pojo.SysRole" id="SysRoleResult">
        <id property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="dataScope" column="data_scope"/>
        <result property="menuCheckStrictly" column="menu_check_strictly"/>
        <result property="deptCheckStrictly" column="dept_check_strictly"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <!-- 查询用户角色信息 -->
    <sql id="selectRoleVo">
        select distinct r.role_id,
                        r.role_name,
                        r.role_key,
                        r.role_sort,
                        r.data_scope,
                        r.menu_check_strictly,
                        r.dept_check_strictly,
                        r.status,
                        r.del_flag,
                        r.create_time,
                        r.remark
        from sys_role r
                 left join sys_user_role ur on ur.role_id = r.role_id
                 left join sys_user u on u.user_id = ur.user_id
                 left join sys_dept d on u.dept_id = d.dept_id
    </sql>

    <insert id="authUser">
        insert into sys_user_role(user_id, role_id) values
        <foreach collection="userIds" item="userId" separator=",">
            (#{userId}, #{roleId})
        </foreach>
    </insert>

    <insert id="addRoleDeptByRoleId">
        insert into sys_role_dept(role_id, dept_id) values
        <foreach collection="deptIds" item="deptId" separator=",">
            (#{roleId},#{deptId} )
        </foreach>
    </insert>

    <delete id="deleteRoleDept">
        delete from sys_role_menu where role_id in
        <foreach collection="array" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </delete>

    <delete id="cancelAuthUser">
        delete from sys_user_role where role_id = #{roleId} and user_id in
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <!-- 根据用户ID查询角色 -->
    <select id="selectRolePermissionByUserId" parameterType="Long" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        WHERE r.del_flag = '0' and ur.user_id = #{userId}
    </select>

    <!-- 查询角色列表信息 -->
    <select id="listEntities" resultType="com.cms.system.domain.vo.SysRoleListVo">
        select r.role_id,
        r.role_name,
        r.role_key,
        count(ur.user_id) userCount,
        r.data_scope,
        r.status,
        r.remark
        from sys_role r
        left join sys_user_role ur on ur.role_id = r.role_id
        <where>
            <if test="roleName != null and roleName != ''">
                and role_name like concat('%',#{roleName},'%')
            </if>
            <if test="dataScope != null">
                and data_scope = #{dataScope}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        group by r.role_id
    </select>


</mapper>
