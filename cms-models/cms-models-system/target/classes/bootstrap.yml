# Tomcat
server:
  port: 10240

# Spring
spring:
  application:
    # 应用名称
    name: cms-system
  profiles:
    # 环境配置
    active: guotest
  cloud:
    nacos:
      # 服务注册、配置拉取地址
      server-addr: 172.20.0.7:8848
      discovery:
        namespace: guotest
        group: cms-project
        username: guoDev
        password: QuJi@21612002.
      config:
        namespace: guotest
        group: cms-project
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - data-id: application-guotest.yml
            group: cms-project
            namespace: guotest
        # nacos的用户名和密码
        username: guoDev
        password: QuJi@21612002.
