# Spring ,配置文件读取逻辑 Data ID格式：<spring.application.name>-<spring.profiles.active>.<file-extension>
spring:
  application:
    # 应用名称
    name: cms-gateway
  profiles:
    # 环境配置
    active: guoA
  cloud:
    nacos:
      server-addr: 10.8.0.99:8848
      # 注册中心配置,作用: 1.服务发现 2.配置中心
      discovery:
        namespace: guoA #todo 开发时候,这里特殊根据自己配置的命名空间来进行填写
        group: cms-project
        username: guoDev
        password: QuJi@21612002.
      config:
        namespace: guoA
        group: cms-project
        # 配置文件格式
        file-extension: yml
        # nacos的用户名和密码
        username: guoDev
        password: QuJi@21612002.
